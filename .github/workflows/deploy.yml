name: Deploy

on:
  push:
    branches:
      - CustomBranch

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
      - run: echo Hello world!